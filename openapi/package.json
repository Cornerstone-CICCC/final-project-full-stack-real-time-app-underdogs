{
  "name": "supportflow",
  "version": "1.0.0",
  "description": "Customer Support Live Chat",
  "scripts": {
    "swagger": "live-server --port=8080 --open=/",
    "mock": "prism mock openapi.yaml --port 4010 --cors"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "@stoplight/prism-cli": "^5.0.0"
  },
  "overrides": {
    "fsevents": "~2.3.2"
  }
}
